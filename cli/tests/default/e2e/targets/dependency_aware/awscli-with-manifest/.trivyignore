# Test fixture for opengrep dependency-aware end-to-end tests.
# Vulnerable dependencies are intentionally present and should NOT be flagged in production scans.
# CVE-2020-14343: Arbitrary code execution in PyYAML < 5.4 via full_load — test fixture only, not a real vulnerability
CVE-2020-14343
# CVE-2017-18342: Arbitrary code execution in PyYAML < 5.1 via yaml.load() — test fixture only, not a real vulnerability
CVE-2017-18342
# CVE-2020-25658: Timing attacks in python-rsa (pkg: rsa) — test fixture only, not a real vulnerability
CVE-2020-25658
# CVE-2020-13757: Python-RSA decryption of ciphertext leads to DoS (pkg: rsa) — test fixture only, not a real vulnerability
CVE-2020-13757