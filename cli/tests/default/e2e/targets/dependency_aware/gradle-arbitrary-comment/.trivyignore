# Test fixture — logback-core 1.2.11 and google-oauth-client are intentionally bundled as targets for dependency-awareness e2e tests.
# Suppressing false-positive CVEs triggered by the fixture lockfile.
CVE-2024-12798

# logback-core 1.2.11 is intentionally bundled for dependency-awareness e2e tests.
# CVE-2025-11226 identifies Arbitrary Code Execution via file processing, but this is a deliberate test fixture target,
# not a runtime application dependency.
CVE-2025-11226

# google-oauth-client is bundled for dependency-awareness e2e tests. CVE-2021-22573 identifies improper signature
# verification, but this is a deliberate test fixture target, not a runtime application dependency.
CVE-2021-22573

# logback-core is bundled for dependency-awareness e2e tests. CVE-2026-1225 (GHSA-qqpg-mvqg-649v) identifies
# class instantiation via Joran configuration, but this is a deliberate test fixture target,
# not a runtime application dependency.
CVE-2026-1225

# io.grpc:grpc-netty-shaded is bundled for dependency-awareness e2e tests. CVE-2025-55163
# identifies the MadeYouReset HTTP/2 DDoS vulnerability in Netty, but this is a deliberate
# test fixture target, not a runtime application dependency.
CVE-2025-55163

# guava is bundled for dependency-awareness e2e tests. CVE-2020-8908 (GHSA-5mg8-w23w-74h3) identifies
# Information Disclosure via temporary directory creation, but this is a deliberate test fixture target,
# not a runtime application dependency.
CVE-2020-8908

# jackson-databind is bundled for dependency-awareness e2e tests. CVE-2022-42004 identifies Uncontrolled Resource
# Consumption, but this is a deliberate test fixture target, not a runtime application dependency.
CVE-2022-42004

# json-path is bundled for dependency-awareness e2e tests. CVE-2023-51074 (GHSA-pfh2-hfmq-phg5) identifies
# Out-of-bounds Write, but this is a deliberate test fixture target, not a runtime application dependency.
CVE-2023-51074

# com.google.protobuf:protobuf-java is bundled for dependency-awareness e2e tests.
# CVE-2022-3510 (GHSA-4gg5-vx3j-xwc7) identifies Uncontrolled Resource Consumption, but this is a deliberate test fixture target,
# not a runtime application dependency.
CVE-2022-3510

# io.projectreactor.netty:reactor-netty-http is bundled for dependency-awareness e2e tests.
# CVE-2022-31684 (GHSA-7w4x-4h67-pgmv) identifies Invalid HTTP requests in Reactor Netty HTTP Server may reveal access tokens,
# but this is a deliberate test fixture target, not a runtime application dependency.
CVE-2022-31684

# org.apache.commons:commons-compress is bundled for dependency-awareness e2e tests.
# CVE-2024-26308 (GHSA-4265-ccf5-phj5) identifies OutOfMemoryError unpacking broken Pack200 file,
# but this is a deliberate test fixture target, not a runtime application dependency.
CVE-2024-26308

# net.minidev:json-smart is bundled for dependency-awareness e2e tests. CVE-2023-1370 (GHSA-493p-pfq6-5258) identifies
# Uncontrolled Recursion, but this is a deliberate test fixture target, not a runtime application dependency.
CVE-2023-1370

# io.netty:netty-handler-proxy is bundled for dependency-awareness e2e tests. CVE-2026-42578 (GHSA-45q3-82m4-75jr) identifies
# HTTP Header Injection via HttpProxyHandler Disabled Validation, but this is a deliberate test fixture target,
# not a runtime application dependency.
CVE-2026-42578
